/*
 * STUBBED for RasterDB — CUDA hybrid scan removed.
 * Parquet reading goes through DuckDB CPU path or rasterdf.
 */
#include <data/host_parquet_representation.hpp>

// No implementation needed — all methods are inline stubs in the header.
