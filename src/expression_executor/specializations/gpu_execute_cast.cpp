/*
 * STUBBED for RasterDB — cudf GPU expression evaluation removed.
 * GPU operations go through rasterdf or fall back to DuckDB CPU.
 */
#include "duckdb/planner/expression/bound_cast_expression.hpp"

// Stub: implementations removed. GPU expressions use rasterdf or CPU fallback.
