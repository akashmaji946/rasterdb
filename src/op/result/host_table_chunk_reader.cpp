/*
 * STUBBED for RasterDB — cudf GPU operations removed.
 * GPU ops go through rasterdf or fall back to DuckDB CPU.
 */
#include <helper/utils.hpp>

// Stub: implementations removed — rasterdf or CPU fallback handles these ops.
