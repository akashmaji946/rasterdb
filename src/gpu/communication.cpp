/*
 * RasterDB GPU stub: GPU communication utilities
 * TODO: Implement using rasterdf Vulkan dispatches.
 * Currently all functions throw to trigger CPU fallback.
 */
#include "../rasterdf/rdf_stubs.hpp"

namespace duckdb {
// Stub — all GPU functions from this translation unit are defined
// in the corresponding header and called from operator .cpp files.
// They will throw at runtime, triggering fallback to DuckDB CPU.
} // namespace duckdb
