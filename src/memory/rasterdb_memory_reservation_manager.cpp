/*
 * STUBBED for RasterDB — CUDA memory reservation manager removed.
 * Memory managed by rasterdf (Vulkan + VMA).
 */
#include <memory/rasterdb_memory_reservation_manager.hpp>

// Stub: all memory reservation is handled by rasterdf's VMA allocator.
